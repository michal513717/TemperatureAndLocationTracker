import type { Request, Response } from "express";

export const measurementController = async (
    req: Request,
    res: Response
) => {

    //TODO implemenet middleware

    return res.status(200).json({  });
};